<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/Teal500Main</item>
        <item name="colorPrimaryDark">@color/Teal900Dark</item>
        <item name="colorAccent">@color/PinkAccent100</item>
    </style>

</resources>
